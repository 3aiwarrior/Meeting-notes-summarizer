[pytest]
minversion = 7.0
addopts =
    -ra
    -q
    --strict-markers
    --cov=app
    --cov-report=term-missing
    --cov-report=html
    --tb=short
testpaths = tests
pythonpath = .
asyncio_mode = auto
